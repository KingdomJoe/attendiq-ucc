# Production API (Render). Override locally with:
#   set ATTENDIQ_DEV=true
#   mvn javafx:run -pl desktop
# or: java -Dapi.url=http://localhost:8080 -jar attendance-desktop.jar
api.url=https://ucc-attendance-system.onrender.com
